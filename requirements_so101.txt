# ============================================================
# SO101 仿真遥操作 / 数据采集 / 模型推理 依赖
#
# 安装步骤：
#   conda create -n so101 python=3.10
#   conda activate so101
#   pip install -e .                                    # 安装 orca-gym 本包
#   pip install -e 3rd_party/robomimic                  # 安装 robomimic
#   pip install -e lerobot                              # 安装 lerobot
#   pip install -e openpi/packages/openpi-client        # 安装 openpi-client
#   pip install -r requirements_so101.txt               # 安装其他依赖
# ============================================================

# ── OrcaGym 运行依赖 ────────────────────────────────────────────────────────
websockets>=11.0
av>=10.0.0
opencv-python>=4.8.0
matplotlib>=3.7.0

# ── 键盘监听 ────────────────────────────────────────────────────────────────
pynput>=1.7.6

# ── 主臂串口驱动（数采流程必须）──────────────────────────────────────────────
pyserial>=3.5
feetech-servo-sdk>=1.0.0

# ── 工具库 ──────────────────────────────────────────────────────────────────
colorama>=2.4.0
scipy>=1.11.0
numpy>=1.24.0
